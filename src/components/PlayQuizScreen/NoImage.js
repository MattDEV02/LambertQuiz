import React from "react";
import { SafeAreaView, View, Text } from "react-native";

const NoImage = () => {
	return (
		<SafeAreaView>
			<View>
				<Text style={{ fontWeight: "bold" }}>No Image</Text>
			</View>
		</SafeAreaView>
	);
};

export default NoImage;
