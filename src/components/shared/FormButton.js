import React from "react";
import { View, Text } from "react-native";

const FormButton = ({
	labelText = "",
	handleOnPress = null,
	style,
	isPrimary = true,
	...more
}) => {
	return (
		<View>
			<Text>
            
         </Text>
		</View>
	);
};

export default FormButton;
